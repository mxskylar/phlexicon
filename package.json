{
    "name": "phlexicon",
    "version": "0.0.0",
    "description": "A phonetic catalog for lexicons: Study a language. Document vocabulary. Analyze accents.",
    "main": "build/main.js",
    "author": "https://github.com/mxskylar",
    "license": "MIT",
    "scripts": {
        "install-theme": "curl -o src/bootstrap.min.css https://bootswatch.com/5/cerulean/bootstrap.min.css",
        "install-js": "curl -o src/bootstrap.bundle.min.js https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js",
        "postinstall": "npm run install-js && npm run install-theme",
        "compile": "rm -rf build && cp -r src build && rm -rf build/*.ts && tsc",
        "start": "npm run compile && electron ."
    },
    "devDependencies": {
        "electron": "^35.0.3",
        "typescript": "^5.8.2"
    }
}
