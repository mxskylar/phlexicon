{
    "name": "phonetic-flashcards",
    "version": "0.0.0",
    "description": "Desktop app to create flashcards for learning languages at a phonetic level.",
    "main": "build/main.js",
    "author": "https://github.com/mxskylar",
    "scripts": {
        "compile": "tsc && cp src/*.html build && cp src/*.css build && cp -r node_modules/xel build",
        "start": "npm run compile && electron ."
    },
    "dependencies": {
        "xel": "^0.33.7"
    },
    "devDependencies": {
        "electron": "^35.0.3",
        "typescript": "^5.8.2"
    }
}
